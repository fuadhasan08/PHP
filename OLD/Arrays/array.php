<?php 

//count(), 

$array = ["Sat", "Sun", "Mon", "Tue", "Wed", "Thu", "Fri"];

foreach ($array as $key => $value) {
    //Both are same
    echo $array[$key] . PHP_EOL;
    echo $value . PHP_EOL;
}

//empty();
//reset array

//array keys work same as foreach -> $array[$key]
//double foreach for multi dimentional arrays
//unset any array element
//array sorting asc and dsc, index array and associative array


